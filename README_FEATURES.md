# Smart Home Application

Современное веб-приложение для управления умным домом с поддержкой тем и многоязычности.

**Дипломный проект** по специальности 09.02.07 "Информационные системы и программирование"

---

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск frontend (терминал 1)
npm run dev

# Запуск backend (терминал 2)
npm run server
```

Открой http://localhost:3000

### Production деплой

Подробная инструкция: [DEPLOYMENT.md](./DEPLOYMENT.md)

**Хостинги:**
- **Frontend**: Netlify (бесплатно)
- **Backend**: Render (бесплатно)
- **База данных**: MongoDB Atlas (бесплатно 512 МБ)

---

## ✨ Новые функции

### 🌓 Переключение темы
- Кнопка смены темы находится в навигационной панели (Navbar)
- Поддерживает светлую (light) и темную (dark) темы
- Автоматически определяет системные настройки при первом запуске
- Сохраняет выбранную тему в localStorage
- Иконка: 🌙 для темной темы, ☀️ для светлой

### 🌐 Переключение языка
- Поддерживаемые языки:
  - 🇬🇧 English (en)
  - 🇷🇺 Русский (ru)
- Кнопка смены языка находится в навигационной панели рядом с переключателем темы
- Автоматически определяет язык браузера при первом запуске
- Сохраняет выбранный язык в localStorage
- Переводы доступны для всех основных интерфейсных элементов

### 🔐 Аутентификация
- Регистрация с подтверждением email (6-значный код)
- Вход по email/паролю
- OAuth (Google, GitHub)
- Восстановление пароля
- JWT токены (7 дней)
- Загрузка аватара

### 📱 Устройства
- 8 типов: свет, термостат, камера, замок, динамик, TV, розетка, датчик
- CRUD операции (создание, чтение, обновление, удаление)
- Включение/выключение
- Избранное
- Фильтрация по комнатам

### 📊 Мониторинг состояний
- Погода (OpenWeatherMap API)
- Температура по комнатам
- Качество воздуха (AQI)
- Энергопотребление
- Безопасность
- Системная информация

### 📝 Логи действий
- История всех операций
- Детализация изменений
- Временные метки

---

## 📡 API

### Аутентификация
```
POST   /api/auth/register          Регистрация
POST   /api/auth/login             Вход
POST   /api/auth/verify            Подтверждение email
POST   /api/auth/forgot-password   Запрос сброса пароля
POST   /api/auth/reset-password    Сброс пароля
GET    /api/auth/me                Текущий пользователь
PUT    /api/auth/profile           Обновление профиля
PUT    /api/auth/password          Смена пароля
POST   /api/auth/avatar            Загрузка аватара
DELETE /api/auth/avatar            Удаление аватара
DELETE /api/auth/account           Удаление аккаунта
```

### Устройства
```
GET    /api/devices                Список устройств
POST   /api/devices                Создать устройство
GET    /api/devices/:id            Получить устройство
PUT    /api/devices/:id            Обновить устройство
DELETE /api/devices/:id            Удалить устройство
```

### Другое
```
GET    /api/weather?city=...       Погода
GET    /api/activity-logs          Логи действий
GET    /api/health                 Health check
GET    /api/oauth/github           Вход через GitHub
GET    /api/oauth/google           Вход через Google
```

---

## 📚 Использование i18n в компонентах

### Подключение i18n store

```vue
<script setup>
import { useI18nStore } from '../stores/i18n'

const i18nStore = useI18nStore()

// Функция перевода
const t = (key) => i18nStore.t(key)
</script>

<template>
  <div>
    <h1>{{ t('dashboard.title') }}</h1>
    <p>{{ t('dashboard.welcome') }}</p>
  </div>
</template>
```

### Основные ключи переводов

```javascript
// Навигация
t('nav.dashboard')      // Панель управления
t('nav.devices')        // Устройства
t('nav.status')         // Состояние
t('nav.profile')        // Профиль
t('nav.logout')         // Выйти

// Dashboard
t('dashboard.hello')           // Привет
t('dashboard.devicesConnected') // Устройств подключено
t('dashboard.addDevice')       // Добавить устройство
t('dashboard.rooms')           // Комнаты
t('dashboard.favorites')       // Избранное

// Устройства
t('devices.title')        // Мои устройства
t('devices.addDevice')    // Добавить устройство
t('devices.edit')         // Редактировать
t('devices.delete')       // Удалить

// Auth
t('auth.login')           // Войти
t('auth.register')        // Регистрация
t('auth.email')           // Email
t('auth.password')        // Пароль
```

### Добавление новых переводов

Откройте файл `src/stores/i18n.js` и добавьте ключи во все языки:

```javascript
const translations = {
  en: {
    mySection: {
      myKey: 'My translation'
    }
  },
  ru: {
    mySection: {
      myKey: 'Мой перевод'
    }
  }
}
```

---

## 🏗️ Структура проекта

```
smart-home-app/
├── src/
│   ├── views/                  # Страницы
│   │   ├── LoginView.vue
│   │   ├── RegisterView.vue
│   │   ├── ForgotPasswordView.vue
│   │   ├── DashboardView.vue
│   │   ├── DevicesView.vue
│   │   ├── ProfileView.vue
│   │   └── StatusView.vue
│   ├── components/             # Компоненты
│   │   ├── Navbar.vue
│   │   ├── ThemeToggle.vue
│   │   └── LanguageSelector.vue
│   ├── stores/                 # Pinia stores
│   │   ├── auth.js
│   │   ├── devices.js
│   │   ├── activityLogs.js
│   │   ├── theme.js
│   │   └── i18n.js
│   ├── router/
│   │   └── index.js
│   ├── services/
│   │   └── api.js
│   ├── assets/
│   ├── App.vue
│   └── main.js
├── server/                     # Backend
│   ├── routes/
│   │   ├── auth.js
│   │   ├── devices.js
│   │   ├── oauth.js
│   │   ├── weather.js
│   │   └── activity-logs.js
│   ├── middleware/
│   │   └── auth.js
│   ├── config/
│   │   ├── database.js
│   │   └── db.js
│   ├── services/
│   │   └── email.js
│   ├── database/
│   │   └── db.json
│   ├── uploads/
│   └── index.js
├── .env.example                # Пример переменных окружения
├── DEPLOYMENT.md               # Инструкция по развёртыванию
├── TECHNICAL_SPECIFICATION.md  # Техническое задание
├── ЭКОНОМИЧЕСКАЯ_ЧАСТЬ.md      # Экономические расчёты
├── package.json
├── vite.config.js
├── vercel.json                 # Конфиг Vercel
└── render.yaml                 # Конфиг Render
```

---

## 🛠️ Технологии

### Frontend
| Технология | Версия | Назначение |
|------------|--------|------------|
| Vue 3 | 3.3+ | Фреймворк |
| Pinia | 2.1+ | State management |
| Vue Router | 4.2+ | Роутинг |
| Axios | 1.13+ | HTTP клиент |
| Vite | 4.4+ | Сборщик |

### Backend
| Технология | Версия | Назначение |
|------------|--------|------------|
| Node.js | 18+ | Платформа |
| Express | 4.18+ | Фреймворк |
| JWT | 9.0+ | Аутентификация |
| bcryptjs | 2.4+ | Хеширование |
| Nodemailer | 8.0+ | Email |
| Multer | 2.0+ | Загрузка файлов |
| MongoDB | 6.3+ | База данных |

---

## 🎨 Темы и стили

### Переключение темы

Тема применяется через CSS переменные:

```css
:root {
  --bg-primary: #ffffff;
  --text-primary: #1e293b;
  --primary-color: #60a5fa;
}

[data-theme="dark"] {
  --bg-primary: #0f172a;
  --text-primary: #f1f5f9;
  --primary-color: #818cf8;
}
```

### Кастомный скроллбар

```css
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-thumb {
  background: #cbd5e1;
  border-radius: 4px;
}
```

---

## 📝 Примечания

### Локальная разработка
- Frontend: http://localhost:3000
- Backend: http://localhost:3001
- API: http://localhost:3001/api

### Production
- Frontend: https://smart-home-app-xxxx.netlify.app
- Backend: https://smart-home-api-xxxx.onrender.com
- API: https://smart-home-api-xxxx.onrender.com/api

### Хранение данных
- **Development**: JSON-файл (`server/database/db.json`)
- **Production**: MongoDB Atlas

### Безопасность
- Пароли хешируются bcryptjs
- JWT токены действительны 7 дней
- Email верификация обязательна
- CORS настроен для production доменов

---

## 📄 Документация

- [README.md](./README.md) - Основная информация
- [DEPLOYMENT.md](./DEPLOYMENT.md) - Развёртывание
- [TECHNICAL_SPECIFICATION.md](./TECHNICAL_SPECIFICATION.md) - ТЗ
- [ЭКОНОМИЧЕСКАЯ_ЧАСТЬ.md](./ЭКОНОМИЧЕСКАЯ_ЧАСТЬ.md) - Расчёты
- [QUICK_START.md](./QUICK_START.md) - Быстрый старт

---

## 👨‍💻 Автор

Студент группы XXX-XXX  
Специальность: 09.02.07 "Информационные системы и программирование"  
Период разработки: 26 января — 23 февраля 2026 г.

---

## 📄 Лицензия

MIT
