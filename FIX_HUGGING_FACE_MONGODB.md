# 🔧 Исправление проблемы с MongoDB на Hugging Face Spaces

## ❌ Проблема

Ваше приложение на Hugging Face использует JSON вместо MongoDB, несмотря на то что `MONGODB_URI` указан в настройках.

**Логи:**
```
✅ JSON Database initialized at: /app/server/database/db.json
📊 Database type: json
```

---

## 🔍 Причина

Hugging Face Spaces с **Docker** передаёт переменные окружения иначе, чем ожидается:

1. Переменные из UI (Settings → Variables) передаются в контейнер при запуске
2. `dotenv` в Node.js ищет файл `.env`, которого нет в Docker-образе
3. `process.env.MONGODB_URI` оказывается `undefined`
4. Приложение падает в fallback режим с JSON

---

## ✅ Решение (по шагам)

### Шаг 1: Проверьте переменные на Hugging Face

1. Зайдите в свой Space: https://huggingface.co/spaces/sweety52-smart-home-api
2. Перейдите на вкладку **Settings**
3. Прокрутите до **Variables and secrets**
4. Убедитесь, что все переменные добавлены:

```
PORT=7860
NODE_ENV=production
BACKEND_URL=https://sweety52-smart-home-api.hf.space
FRONTEND_URL=https://tox1k332.github.io/smart-home-app
MONGODB_URI=mongodb+srv://smarthome:3Ej0jaDEXvCBEQZN@cluster0.pktf6p2.mongodb.net/?appName=Cluster0
JWT_SECRET=smarthome-hf-secret-key-2026-change-this
OPENWEATHER_API_KEY=46954c93e53c9992375e3904ec3618a7
```

**Важно:** 
- Имя переменной должно быть точно `MONGODB_URI` (без кавычек)
- Значение должно быть точно вашей строкой подключения
- Убедитесь, что нет лишних пробелов

### Шаг 2: Обновите файлы проекта

Я уже обновил следующие файлы:

✅ `server/config/db.js` - улучшенная обработка подключения с логами
✅ `server/index.js` - вывод информации о переменных окружения
✅ `Dockerfile` - обновлён для лучшей совместимости
✅ `.dockerignore` - исключает лишние файлы

### Шаг 3: Запушьте изменения в репозиторий

```bash
cd d:\vue\smart-home-app
git add .
git commit -m "Fix MongoDB connection on Hugging Face Spaces"
git push
```

### Шаг 4: Перезапустите Space на Hugging Face

1. Зайдите в свой Space: https://huggingface.co/spaces/sweety52-smart-home-api
2. Перейдите на вкладку **Settings**
3. Прокрутите до **Factory reboot**
4. Нажмите **Reboot Space**

ИЛИ удалите и создайте Space заново (если не помогает).

### Шаг 5: Проверьте логи

1. Зайдите в свой Space
2. Перейдите на вкладку **Logs** (или **App** → **Logs**)
3. Дождитесь запуска (~2-3 минуты)

**Ожидаемые логи:**
```
🔍 Environment check:
  PORT: 7860
  NODE_ENV: production
  MONGODB_URI: ✅ Set
  BACKEND_URL: https://sweety52-smart-home-api.hf.space
  FRONTEND_URL: https://tox1k332.github.io/smart-home-app
  OPENWEATHER_API_KEY: ✅ Set

🔍 MONGODB_URI found: mongodb+srv://smarthome:***@cluster0.pktf6p2.mongodb.net/...
🔄 Connecting to MongoDB...
✅ MongoDB connected successfully
📊 Database type: mongodb
```

---

## 🚨 Если всё ещё не работает

### Проверка 1: URI содержит специальные символы

Если ваш пароль содержит специальные символы (`@`, `#`, `$`, `%`, `&`, `*`), их нужно **URL-кодировать**.

**Пример:**
- Было: `password@123`
- Стало: `password%40123`

Таблица кодирования:
| Символ | Код |
|--------|-----|
| @ | %40 |
| # | %23 |
| $ | %24 |
| % | %25 |
| & | %26 |
| * | %2A |

Ваш текущий пароль: `3Ej0jaDEXvCBEQZN` - не содержит специальных символов, всё OK ✅

### Проверка 2: MongoDB Atlas разрешает подключения

1. Зайдите на https://cloud.mongodb.com
2. Выберите свой кластер
3. **Network Access**
4. Убедитесь, что есть правило: `0.0.0.0/0` (Allow from anywhere)

ИЛИ добавьте IP Hugging Face (если известен).

### Проверка 3: Проверьте строку подключения локально

Создайте файл `test-mongo.js` в корне проекта:

```javascript
const { MongoClient } = require('mongodb')

const uri = 'mongodb+srv://smarthome:3Ej0jaDEXvCBEQZN@cluster0.pktf6p2.mongodb.net/?appName=Cluster0'

async function test() {
  try {
    console.log('Testing MongoDB connection...')
    const client = new MongoClient(uri, {
      serverSelectionTimeoutMS: 5000
    })
    await client.connect()
    await client.db().admin().ping()
    console.log('✅ MongoDB connection SUCCESS')
    await client.close()
  } catch (error) {
    console.error('❌ MongoDB connection FAILED:', error.message)
  }
}

test()
```

Запустите:
```bash
node test-mongo.js
```

Если локально работает, но на HF нет - проблема в настройках HF Spaces.

### Проверка 4: Используйте Dockerfile.hf

Переименуйте `Dockerfile.hf` в `Dockerfile` (сделайте основным):

```bash
cd d:\vue\smart-home-app
move Dockerfile Dockerfile.backup
move Dockerfile.hf Dockerfile
git add .
git commit -m "Use Dockerfile.hf for Hugging Face"
git push
```

Затем перезапустите Space (Factory reboot).

---

## 📊 Альтернативное решение

Если Hugging Face Spaces продолжает игнорировать переменные окружения, рассмотрите другие варианты:

### Вариант A: Render.com (бесплатно)

1. Зарегистрируйтесь на https://render.com
2. Создайте **Web Service**
3. Подключите GitHub репозиторий
4. Укажите переменные окружения в UI Render
5. Render лучше работает с переменными окружения

### Вариант B: Railway.app (бесплатно с лимитами)

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Подключите GitHub
4. Добавьте переменные окружения

### Вариант C: Vercel (бесплатно)

1. Зарегистрируйтесь на https://vercel.com
2. Импортируйте проект
3. Добавьте переменные окружения
4. Vercel автоматически соберёт backend

---

## 🎯 Быстрая проверка после исправления

1. Откройте: https://sweety52-smart-home-api.hf.space/api/health
2. Должно вернуться: `{"status": "ok", ...}`

3. Откройте консоль браузера на странице frontend
4. Попробуйте войти/зарегистрироваться
5. Проверьте Network tab - запросы должны идти на HF Spaces

6. Проверьте логи HF Spaces - должно быть:
   ```
   📊 Database type: mongodb
   ```

---

## 📞 Если нужна помощь

1. Проверьте логи на Hugging Face: вкладка **Logs**
2. Скопируйте первые 50 строк лога
3. Покажите ошибку - помогу разобраться

---

**Удачи! MongoDB должен заработать после этих шагов! 🍀**
